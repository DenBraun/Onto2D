# Textual Transmission Lab

This light-theme Explorer renders the verified `manuscript-stemmatics-v1` artifact. It keeps recorded readings, derived selected-site agreement, published reconstruction, contamination, uncertainty, and analysis boundaries distinct.

The artifact is loaded from the same origin with an exact SHA-256 pin, byte limit, redirect rejection, strict UTF-8 decoding, and fail-closed browser validation. The Model Studio navigation selects the exact `manuscript-transmission@v1-4581c6819fd2ab28` release.
