# Airflow Constraint Resolution Lab

Static source-locked Explorer for `airflow-dependency-constraints-v1`.

The browser verifies the exact case artifact before rendering the finite
solution census, three Historical Load profiles, optimum witnesses, shared
dependencies, resolver diagnostics, and constraint ablations.
