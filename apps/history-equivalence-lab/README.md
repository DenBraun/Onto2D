# History Equivalence Lab

This light-theme Explorer loads the exact frozen Reproducible Build Equivalence
artifact, verifies its SHA-256 before parsing, validates its closed semantic
inventory, and exposes three captured history pairs under five independent
equivalence regimes.

The Model Studio link selects the exact registered
`reproducible-build-equivalence@v1-78148e4e627d2c9f` release.
