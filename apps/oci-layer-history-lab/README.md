# OCI Layer History Lab

Light-theme browser explorer for the exact `oci-layer-history-v1` artifact. It
shows native layer timelines, normalized rootfs convergence, hidden/deleted
history, identity-regime disagreement, and four cost-relative Historical Load
results without mutating the verified artifact.
