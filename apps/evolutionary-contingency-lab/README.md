# Evolutionary Contingency Lab

Light-theme explorer for the exact LTEE Ara-3 citrate replay artifact. It verifies the artifact bytes before rendering, keeps all replay protocols separate, and exposes observed, not-observed, and not-run cells as different states.

Open `/apps/evolutionary-contingency-lab/` from the shared static server.
