# SG2-013 declared typed controls

These are constructed computational controls, fixed before the typed reference
and production artifact runs. They are not evidence of common real mechanisms.

The existing typed regime supplies all five edge fields and the six-node bound.
Two complete field tuples A/B and absent edges define the exhaustive 1–3 node
census. Independent Python node permutations must agree with production class
partitions, without assuming the same canonical numbering. All 720 relabelings
of the declared six-node graph must retain its value.

Single-field changes must split an otherwise identical single edge. Correlated
and crossed reciprocal edges must split under one joint node bijection even
though each field independently permits a matching. Set order must not split;
explicit empty sets are observed, whereas absent fields leave a null typed
observation. Cycles, isolates and complete six-node graphs remain supported.

Code-renumbering controls declare 0 ↔ 20, 1 ↔ 10 and 2 ↔ 30 in each coded field.
Identity and role-renaming controls are also explicit in controls.json. Test
policy authorizes a mapping only in cases with approve=true. This is synthetic
test authorization, not a claim of completed external scientific review.
Creating a mapping never supplies its approval. A partial mapping must remain
unresolved even with test authorization. Recanonicalize after translation.

Equal dictionary bytes across different source bindings do not authorize
alignment. The unchanged Causal Emergence release contributes only the explicit
six-node scope listed in controls.json. No cross-domain mapping or empirical
result is inferred. No pairwise distance, final comparison status, probe or
response signature is supplied by this stage.
